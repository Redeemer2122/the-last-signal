# QA strategy

The project uses `astro check`, the production build, deterministic media validation, and browser-driven visual QA. Temporary screenshots and browser artifacts belong under `output/` and are intentionally gitignored.
